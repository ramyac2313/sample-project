As part of interview process to be considered for SDET, we would like for you to complete a sample project. 
Please complete the following assignment and send link for project’s on GitHub at the earliest that will work for you.

1. Given the endpoint,  http://ec2-54-84-52-184.compute-1.amazonaws.com:3000/
§ Define and Implement Automated Tests, for posts, comments and users Resources using GET, POST PUT http  Methods.
//PUT method:
String uri = "http://ec2-54-84-52-184.compute-1.amazonaws.com:3000/";
Response resp = given().body(file_loc).with().contentType(ContentType.JSON).accept("application/json").put(uri);
System.out.println(resp.asString());
JsonPath jp = new JsonPath(resp.asString());
String id = jp.get("title").toString();
System.out.println(id);

//POST method:
Response resp = given().authentication().preemptive().basic({username}, {password}).spec(requestSpec).when().post(uri);
System.out.println(resp.asString());
JsonPath jp = new JsonPath(resp.asString());
String id = jp.get("title").toString();
System.out.println(id); 

//GET method:
Response resp = get("http://dummy.restapiexample.com/api/v1/employees");
int code = resp.getStatusCode();
System.out.println(code);
System.out.println(resp.getTime());
System.out.println(resp.asString());

§ Demonstrate usage of best practices in API test automation 
1. Common Library for Repeating Tests
2. Ensure Clarity for Test Steps
3. Keep a Blueprint of the Tests
4. Split the Tests for Ease of Execution

